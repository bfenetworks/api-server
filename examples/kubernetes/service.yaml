apiVersion: v1
kind: Service
metadata:
  name: api-server
spec:
  selector:
    app: api-server
  ports:
    - name: http
      port: 8183
      targetPort: 8183
      nodePort: 30083
    - name: monitor
      port: 8284
      targetPort: 8284
  type: NodePort 
